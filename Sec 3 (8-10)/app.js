const fs = require('fs')
// const name = require('./utils')
const add = require('./utils')

// fs.writeFileSync('notes.txt', 'My name is ' + name)

const sum = add(4, -3)
console.log(sum)

// require('./challange')